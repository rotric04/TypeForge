name: Keep Backend Alive

on:
  schedule:
    # Runs every 10 minutes (using standard cron scheduling)
    - cron: '*/10 * * * *'
  workflow_dispatch: # allows manual triggering

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: |
          curl -s https://typeforge-tkw8.onrender.com/health
